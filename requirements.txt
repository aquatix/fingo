# Command line parameters
click
# Config files, image metadata files
strictyaml
# Image metadata extraction
ExifRead
ImageHash
# Resizing
Pillow
# Address lookups and such
requests
numpy>=1.22.2 # not directly required, pinned by Snyk to avoid a vulnerability
